# AnyPortal Deployment Guide

## 1. Localhost Quick Start
```bash
cd dead-stock-agent
python server.py
```
Visit `http://localhost:8001` in your browser.

## 2. Docker Deployment
```bash
docker-compose up --build -d
```

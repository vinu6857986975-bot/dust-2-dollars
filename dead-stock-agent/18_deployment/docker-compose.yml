version: '3.8'
services:
  anyportal-agent:
    build: .
    ports:
      - "8001:8001"
    environment:
      - PORT=8001
      - PYTHONUNBUFFERED=1
    volumes:
      - ./database:/app/database
    restart: unless-stopped
